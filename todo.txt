- dir listing
- cgi
- alt location
- post and delete requests

- standardfile per loc

test catch of server handling (shutting down servers on running main, keeping the rest)

- A request to your server should never hang forever.
actively tell clients wanting to connect on connection limit reached to fuck off

- handle server names and servers listening on same port
		The first server for a host:port will be the default for this host:port (that means
		it will answer to all the requests that don’t belong to an other server).
